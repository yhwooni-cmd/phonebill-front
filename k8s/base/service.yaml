apiVersion: v1
kind: Service
metadata:
  name: phonebill-front
  labels:
    app: phonebill-front
spec:
  selector:
    app: phonebill-front
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP